<h1 class="fs-4 fw-bold">Posts feeds</h1>

<% @posts.each do |post| %>
  <%= render partial: 'post_card', locals: {post: post} %>
<% end %>
<section class='tasks_section d-flex flex-column align-items-center'>
  <%= render 'post' %>
</section>
